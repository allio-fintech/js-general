import { NextPage } from 'next';
import MarketGraph from 'features/marketGraph/MarketGraph';

const MarketGraphPage: NextPage = () => (
  <main>
    <MarketGraph />
  </main>
);

export default MarketGraphPage;
